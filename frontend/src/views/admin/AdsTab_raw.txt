          <div v-if="activeTab === 'ads'" class="ads-tab">
            <div class="panel-head-row">
              <h3>广告管理</h3>
              <button class="btn btn-primary btn-sm" @click="openCreateAd" :disabled="adsList.length >= 5">+ 添加广告</button>
            </div>
            <div class="ads-help">
              <p>最多 5 个广告位，显示在首页底部，点击名称跳转到对应 HTML 页面。</p>
              <p><strong>图片说明：</strong>HTML 中的外部图片 URL（https://...）可直接使用；本地图片请用编辑器中的「插入图片」按钮上传，会自动转为 base64 内联存储（单张不超过 2MB）。</p>
            </div>

            <div v-if="loadingAds" class="loading-row"><div class="spinner-sm"></div><span>加载中...</span></div>

            <div v-else-if="adsList.length === 0" class="empty-card">
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#cbd5e1" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="3" y1="9" x2="21" y2="9"/><line x1="9" y1="3" x2="9" y2="21"/></svg>
              <p>暂无广告</p>
              <button class="btn btn-primary btn-sm" @click="openCreateAd">添加第一个广告</button>
            </div>

            <div v-else class="table-wrap">
              <table class="data-table">
                <thead>
                  <tr>
                    <th style="width:60px">位置</th>
                    <th>广告名称</th>
                    <th>HTML 预览</th>
                    <th style="width:80px">状态</th>
                    <th style="width:180px">操作</th>
                  </tr>
                </thead>
                <tbody>
                  <tr v-for="ad in adsList" :key="ad.id">
                    <td><span class="slot-badge">{{ ad.slot }}</span></td>
                    <td class="ad-name-cell">{{ ad.name }}</td>
                    <td class="ad-preview-cell">{{ ad.html_content.replace(/<[^>]*>/g, '').substring(0, 60) }}{{ ad.html_content.length > 60 ? '...' : '' }}</td>
                    <td>
                      <span :class="['status-tag', ad.is_active ? 'active' : 'inactive']" @click="toggleAdActive(ad)">
                        {{ ad.is_active ? '展示中' : '已隐藏' }}
                      </span>
                    </td>
                    <td>
                      <div class="row-actions">
                        <button class="btn btn-xs btn-ghost" @click="openEditAd(ad)">编辑</button>
                        <button class="btn btn-xs btn-ghost btn-danger" @click="deleteAd(ad)">删除</button>
                      </div>
                    </td>
                  </tr>
                </tbody>
              </table>
            </div>

            <!-- 广告编辑弹窗 -->
            <div v-if="showAdModal" class="modal-overlay" @click.self="showAdModal = false">
              <div class="modal modal-wide">
                <h3>{{ editingAd ? '编辑广告' : '添加广告' }}</h3>
                <div class="field" v-if="!editingAd">
                  <label>广告位编号</label>
                  <div class="slot-auto">自动分配为 <strong>位置 {{ adForm.slot }}</strong></div>
                </div>
                <div class="field">
                  <label>广告名称（显示在首页底部的链接文字）</label>
                  <input v-model="adForm.name" type="text" placeholder="例如：代理推广" />
                </div>
                <div class="field">
                  <label>HTML 内容</label>
                  <div class="ad-upload-row">
                    <textarea v-model="adForm.html_content" rows="10" class="ad-html-textarea" placeholder="粘贴完整的 HTML 代码，或上传 HTML 文件..."></textarea>
                  </div>
                  <div class="ad-upload-actions">
                    <input ref="adFileInput" type="file" accept=".html,.htm" style="display:none" @change="onAdFileChange" />
                    <input ref="adImgInput" type="file" accept="image/*" style="display:none" @change="onAdImgChange" />
                    <button class="btn btn-sm btn-outline" :disabled="adFileUploading" @click="triggerAdFileUpload">
                      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
                      <span>上传 HTML</span>
                    </button>
                    <button class="btn btn-sm btn-outline" :disabled="adFileUploading" @click="triggerAdImgUpload">
                      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
                      <span>插入图片</span>
                    </button>
                    <span class="ad-file-hint">图片自动转为内联 base64，不超过 2MB</span>
                  </div>
                </div>
                <div class="modal-actions">
                  <button class="btn btn-ghost" @click="showAdModal = false">取消</button>
                  <button class="btn btn-primary" :disabled="savingAd" @click="saveAd">{{ savingAd ? '保存中...' : '保存' }}</button>
                </div>
              </div>
            </div>
          </div>
